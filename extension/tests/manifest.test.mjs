import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';const m=JSON.parse(await readFile(new URL('../manifest.json',import.meta.url),'utf8'));test('manifest is MV3 with popup and module service worker',()=>{assert.equal(m.manifest_version,3);assert.equal(m.action.default_popup,'popup.html');assert.deepEqual(m.background,{service_worker:'src/background.js',type:'module'});});test('manifest uses click-scoped injection and only required service hosts',()=>{assert.deepEqual([...m.permissions].sort(),['activeTab','scripting','storage']);assert.deepEqual(m.host_permissions,['https://api.deepseek.com/*','http://localhost:3000/*','http://127.0.0.1:3000/*']);assert.equal(m.content_scripts,undefined);});
