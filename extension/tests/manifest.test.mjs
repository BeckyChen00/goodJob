import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';const m=JSON.parse(await readFile(new URL('../manifest.json',import.meta.url),'utf8'));test('manifest is MV3 with popup',()=>{assert.equal(m.manifest_version,3);assert.equal(m.action.default_popup,'popup.html');});test('manifest uses click-scoped injection without host access',()=>{assert.deepEqual([...m.permissions].sort(),['activeTab','scripting','storage']);assert.equal(m.host_permissions,undefined);assert.equal(m.content_scripts,undefined);});