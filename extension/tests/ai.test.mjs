import assert from'node:assert/strict';import test from'node:test';import{fillWithAi,parseAiResponse,PROVIDERS}from'../src/ai.js';
test('DeepSeek is the default fixed provider',()=>{assert.equal(PROVIDERS.deepseek.baseUrl,'https://api.deepseek.com');assert.equal(PROVIDERS.deepseek.defaultModel,'deepseek-chat');});
test('AI JSON is reduced to editable form fields',()=>{assert.deepEqual(parseAiResponse('```json\n{"companyName":" 字节跳动 ","jobTitle":"测试开发","ignored":"x"}\n```'),{companyName:'字节跳动',companyShortName:'',companyWebsite:'',jobTitle:'测试开发',location:'',batch:'',sourceUrl:'',notes:''});assert.throws(()=>parseAiResponse('nope'),/有效 JSON/);});
test('AI call requires key and builds DeepSeek request',async()=>{await assert.rejects(()=>fillWithAi({settings:{provider:'deepseek',apiKey:''},pageContext:{}}),/API Key/);let request;const result=await fillWithAi({settings:{provider:'deepseek',apiKey:'secret',model:'deepseek-chat'},pageContext:{url:'https://jobs.example/1',title:'Engineer',html:'<main>JD</main>'},fetchImpl:async(url,options)=>{request={url,options};return{ok:true,json:async()=>({choices:[{message:{content:'{"companyName":"Example","jobTitle":"Engineer"}'}}]})};}});assert.equal(request.url,'https://api.deepseek.com/chat/completions');assert.equal(request.options.headers.Authorization,'Bearer secret');assert.equal(result.jobTitle,'Engineer');});
