# 仓库简历资料

`resume-profile.json` 是插件可导入的、受版本控制的简历 key-value 文档。直接修改每项的 `value`，也可以按相同结构增加字段；`key` 应使用稳定的英文点分命名，`label` 是人工审核时显示的中文名称。

注意：该文件会以明文进入 Git 历史。请仅在用户明确接受这一风险的私有仓库中填写身份证号、手机号、住址等敏感信息。删除当前文件中的敏感值不能清除历史提交里的副本。

插件只把此文件作为默认字段或显式导入来源，不应在构建、打开页面或提交表单时擅自覆盖用户已保存的资料。
