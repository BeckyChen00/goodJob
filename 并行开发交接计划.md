# 求职投递台：三人并行开发交接计划

## 0. 并行启动门禁

当前 Git 仓库尚无提交，绝大多数文件未跟踪，并同时存在 npm 与 pnpm 锁文件。负责人必须先完成以下工作，再允许三位开发者开始：

1. 确认唯一包管理器，建议统一使用 npm，保留 `package-lock.json`，移除 `pnpm-lock.yaml`、`pnpm-workspace.yaml` 和本项目生成的 `.pnpm-store/`。
2. 修正或替换 `tests/rendered-html.test.mjs`。该文件仍测试已删除的 starter 骨架，不能作为当前产品测试。
3. 确认 `.gitignore` 覆盖 `node_modules/`、`.next/`、`.vinext/`、`dist/`、`.wrangler/`、`.pnpm-store/`、覆盖率和本地临时文件。
4. 执行干净安装、静态检查、产品测试和生产构建。
5. 将当前可运行版本提交为唯一基线，建议打标签 `v0.1.0-baseline`。
6. 冻结下方共享接口约定。冻结后任何字段或数据库版本变更必须由负责人审批。

在基线提交完成前，禁止三人分别复制当前未提交目录开始开发。

## 1. 冻结的共享约定

### 数据对象

- `Company` 与 `Job` 字段以 `app/storage.ts` 为准。
- 保持 `Company.id`、`Job.id`、`Job.companyId` 语义不变。
- IndexedDB 名称保持 `job-application-tracker`，当前版本为 1。
- 任何新增字段必须先定义：类型、默认值、是否必填、旧数据兼容策略、导入导出行为。
- 未经审批不得升级 `DB_VERSION`，不得重命名对象仓库或索引。

### 交换格式

- JSON 顶层保持 `{ version, exportedAt, companies, jobs }`。
- 所有导入数据必须校验后再写入；失败不得部分覆盖现有数据。
- 插件与主应用之间只依赖共享 DTO，不直接导入 React UI 文件。

### 代码边界

- 负责人拥有共享文件：`package.json`、锁文件、`app/storage.ts`、`app/page.tsx`、`app/globals.css`、两份项目文档。
- 开发者需要修改共享文件时，先提交一个小型接口提案，由负责人落地或分配唯一写入者。
- 每个任务必须使用 `skills/develop-and-verify`；测试通过后由负责人统一使用 `skills/record-project-docs` 更新文档。

## 2. 开发者 A：数据可靠性与自动化测试

### 目标

建立可信的数据导入、导出、重复检测和回归测试基础，为其他两个任务提供稳定契约。

### 独占目录

- 建议新增 `app/data/`：类型、校验、序列化、重复检测等纯函数。
- 建议新增 `tests/data/`：Node 测试。
- 如需 IndexedDB 模拟库，先向负责人提交依赖申请，不自行修改锁文件。

### 功能范围

1. 将纯数据规则从 `app/storage.ts` 中分离为可测试模块。
2. 为 JSON 导入增加结构、字段类型、关联关系和版本校验。
3. 实现原子导入：校验全部成功后才覆盖数据。
4. 提供重复企业与重复岗位检测函数：
   - 企业优先按规范化域名，其次按名称和简称。
   - 岗位按 `companyId + 规范化 URL`，无 URL 时按企业、岗位名、批次、地点组合。
5. 建立当前产品测试，替换 starter 骨架测试。

### 验收用例

- 有效 v1 备份可完整导入导出。
- 缺字段、错误类型、未知版本、孤儿岗位均拒绝导入且原数据不变。
- URL 大小写、协议、末尾斜杠差异不会漏掉明显重复项。
- 删除企业仍会级联删除岗位。
- `npm run lint`、针对性测试、`npm test`、`npm run build` 全部通过。

### 禁止范围

- 不开发浏览器插件。
- 不改主页面视觉布局。
- 不自行升级 IndexedDB 版本。

## 3. 开发者 B：投递进度、统计与截止提醒 UI

### 目标

增强主应用内的投递管理体验，但首轮不改变持久化模型，尽量基于现有 `Job` 字段完成。

### 独占目录

- 建议新增 `app/components/dashboard/`。
- 建议新增 `app/components/jobs/`。
- 建议新增 `app/domain/application-metrics.ts` 及对应测试。

### 功能范围

1. 增加投递状态筛选和岗位名称、地点、批次搜索。
2. 增加统计卡片：待投、进行中、Offer、已结束。
3. 增加截止状态：已截止、7 天内截止、30 天内截止。
4. 增加志愿限制警告：达到或超过上限时给出非阻断提醒。
5. 将 `app/page.tsx` 的展示组件逐步拆分，保持行为不变。

### 验收用例

- 多筛选条件组合结果正确，清空筛选可恢复全部记录。
- 空日期、无上限、上限为 0、恰好达到上限、超过上限均有明确行为。
- 日期计算使用本地日历日期，避免时区导致提前或延后一天。
- 无岗位、长 JD、长企业名和移动端布局可用。
- 静态检查、领域函数测试、产品测试和生产构建通过。

### 禁止范围

- 不修改 `Company`、`Job` 字段。
- 不开发附件二进制存储。
- 不开发插件通信。

## 4. 开发者 C：Chromium 插件外壳与采集表单

### 目标

完成 Manifest V3 插件的独立外壳和人工录入体验。本轮先使用清晰的适配器接口，不直接写主应用 IndexedDB。

### 独占目录

- 新增 `extension/`，包含 manifest、popup、content script、共享类型、测试和安装说明。
- 插件构建产物只能生成到 `extension/dist/`，并加入忽略列表。

### 功能范围

1. Chrome/Edge Manifest V3 基础配置。
2. 点击插件后读取当前页面 URL、标题和域名。
3. 弹窗支持填写企业父条目与岗位子条目草稿。
4. 实现企业候选匹配纯函数，但暂不直接修改主应用数据库。
5. 定义 `CapturedApplicationDraft` DTO 和 `ApplicationSink` 接口。
6. 首轮使用 `chrome.storage.local` 保存待同步草稿；提供查看、重试和删除草稿的界面。

### 验收用例

- 普通 HTTPS 页面可读取标题和 URL。
- `chrome://`、空标题、超长 URL、无权限页面有明确降级提示。
- 弹窗关闭重开后未同步草稿仍存在。
- 重复点击保存不会生成无法识别的重复记录。
- manifest 校验、类型检查、匹配函数测试和插件构建通过。

### 禁止范围

- 不直接访问主应用 IndexedDB；扩展源与 `localhost:3000` 源不同，无法共享同一 IndexedDB。
- 不申请 `<all_urls>` 之外的多余权限；若能用 `activeTab` 完成则优先使用最小权限。
- 不针对各招聘网站实现大量脆弱的 DOM 解析规则。
- 不修改主应用页面和存储层。

## 5. 负责人保留的串行集成工作

三项并行开发完成后，由负责人按以下顺序集成：

1. 先合并开发者 A，稳定数据契约和测试基线。
2. 再合并开发者 B，处理主应用组件拆分和展示冲突。
3. 最后合并开发者 C，基于 A 的校验/重复检测契约设计正式同步桥接。
4. 决定插件同步方案：管理页导入草稿、扩展消息桥接、本地 API 或云端服务。此决策未完成前不允许插件直接写正式数据。
5. 执行全量静态检查、测试、构建与人工冒烟测试。
6. 通过后更新 `功能迭代文档.md` 和 `技术与需求交接文档.md`。

## 6. 防止工作区污染

### 分支与工作树

- 每位开发者使用独立分支：`codex/data-quality`、`codex/application-ui`、`codex/browser-extension`。
- 每位开发者使用独立 `git worktree` 目录，禁止三人共享同一个物理工作目录。
- worktree 必须建在项目目录之外，例如同级目录 `投递工作-worktrees/<task>`，不得嵌套在仓库内。
- 一个任务一个分支；禁止在任务分支混入格式化全仓库、升级依赖或无关重构。

### 依赖与锁文件

- 全项目只使用一种包管理器和一个锁文件。
- 未获负责人同意不得新增、升级或删除依赖。
- 依赖变更单独提交，说明用途、版本和替代方案。
- 不提交 `node_modules`、构建产物、缓存、覆盖率、日志或浏览器配置。

### 文件所有权与冲突

- 共享文件默认只由负责人修改。
- 禁止对 `app/page.tsx`、`app/globals.css` 做全文件格式化；只修改任务所需最小范围。
- 新功能优先写入新的独占模块，再由负责人以薄适配层接入共享文件。
- 合并前先同步基线并解决冲突；不得使用强制推送覆盖他人分支。

### 数据与测试隔离

- 自动化测试不得使用开发者日常浏览器中的 IndexedDB。
- 测试使用内存实现、临时数据库名或独立浏览器 profile，并在测试后清理。
- 不把真实简历、个人信息、浏览器 profile、导出的真实 JSON 或招聘网站登录信息提交仓库。
- Fixture 必须使用虚构企业和岗位数据，不使用用户真实数据。
- 测试不得依赖固定的本机绝对路径、固定端口或开发者用户名。

### 提交规范

- 每个提交只包含一个可解释的逻辑变更。
- 提交前检查 `git status --short` 和 `git diff --check`。
- PR 必须列出验收项、测试命令、结果、数据迁移影响和未解决风险。
- 文档记录由负责人在集成测试通过后统一更新，避免三条分支同时修改同一文档造成冲突或记录未上线功能。

## 7. 每位开发者交付模板

```markdown
### 完成内容
- （填写内容）

### 改动边界
- 新增文件：
- 修改共享文件：无 / 列表及原因
- 数据模型或版本变化：无 / 说明

### 测试用例
- 正常路径：
- 边界条件：
- 错误路径：
- 回归验证：

### 实际验证
- `命令`：通过 / 失败

### 风险与集成说明
- （填写内容）
```
