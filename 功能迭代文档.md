# 功能迭代文档

只记录已经实现且通过必要测试的功能，最新记录置于顶部。

## 迭代记录

## [2026-08-13] 并行开发基线加固

- 移除已废弃的 starter 预览组件，产品代码不再保留加载骨架残留。
- 将旧 starter 测试替换为求职投递台服务端渲染及核心父子条目、IndexedDB 流程冒烟测试。
- 修复企业卡片展开交互的键盘可访问性、外链安全属性及表单和存储边界类型。
- 统一以 npm 和 `package-lock.json` 作为依赖基线；`npm run lint`、`npm test` 和独立 `npm run build` 均通过。

## [2026-08-13] 企业分类两级导航

- 左侧导航调整为“企业性质 → 行业分类”两级结构。
- 支持一级展开、折叠和数量展示，并支持按一级或二级筛选。
- 生产构建验证通过。

## [2026-08-13] 求职投递台首版

- 建立企业父条目与岗位子条目的本地投递管理页面。
- 支持企业和岗位增删查改、搜索筛选、投递状态、志愿限制、截止日期、JD、简历版本及面试进度。
- 使用 IndexedDB 自动保存，预置 37 家重点国央企，支持 JSON 全量备份恢复。
- 生产构建验证通过。
