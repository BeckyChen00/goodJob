# 功能迭代文档

只记录已经实现且通过必要测试的功能，最新记录置于顶部。

## 迭代记录

## [2026-09-09] 抽屉模式页面上下文桥接修复

- 修复右侧抽屉在得物等招聘站点中无法读取当前页面地址的问题；抽屉不再完全依赖 `chrome.tabs.query` 获取 URL。
- `extension/src/drawer.js` 新增页面上下文桥接：注入到招聘页后可直接读取当前页面 `location.href`、`document.title`、可见正文 `text` 与裁剪后的 `html`，并通过 `postMessage` 返回给抽屉 iframe。
- `extension/src/popup.js` 在抽屉模式下优先通过 `REQUEST_PAGE_CONTEXT/PAGE_CONTEXT` 获取页面上下文，失败时再回退到原有 tabs/content-script 方案。
- 页面正文采集会排除投递台抽屉自身节点，避免把插件表单文字误混入 JD。
- 本轮验证：`npm run build:extension` 通过；Drawer/AI/Capture/Popup 定向测试 9/9 通过；全扩展测试 49/49 通过；`npm run lint` 通过。

## [2026-09-09] AI 一键填写空回填修复

- 修复扩展 `ai.js`、`background.js`、`popup.js` 中的 UTF-8 乱码文案，避免错误提示、AI prompt 和用户反馈出现不可读内容。
- `AI 一键填写` 点击后会立即重新采集当前招聘页上下文，再启动后台任务，避免抽屉打开较早、页面后续加载 JD 后仍使用旧空缓存。
- DeepSeek 招聘页提取 prompt 新增“页面可见正文”输入，并保留 HTML 摘要兜底，提升对岗位详情页的公司、岗位、地点、批次和 JD 识别能力。
- AI 返回 JSON 若所有可编辑字段均为空，将被判定为识别失败并显示错误，不再出现“AI 已填写表单”但页面字段仍为空的假成功状态。
- 本轮验证：`npm run build:extension` 通过；AI/Popup/Drawer 定向测试 9/9 通过；全扩展测试 49/49 通过；`npm run lint` 通过。

## [2026-09-09] 岗位匹配 JD 捕获与浏览器流式输出修复

- 修复插件岗位匹配链路中的 UTF-8 乱码与损坏模板字符串，恢复 Manifest、抽屉 HTML、岗位状态、采集逻辑、弹窗提示与错误信息为可读中文。
- 页面采集脚本新增可见正文 `text` 输出，并继续保留裁剪后的 `html` 兜底；岗位匹配请求会把 title、URL、表单字段、正文文本和 HTML 摘要一起发送到本地 prompt 服务。
- 本地 `scripts/job-fit-server.mjs` 改为只负责读取写死简历路由、按岗位信息判断“研发 / 产品 / 测试”、构造岗位-简历匹配 prompt；LLM 请求由浏览器扩展按已配置 DeepSeek API Key 直接发起并读取 SSE token。
- 岗位匹配结果区新增 `JD 字符数`，便于冒烟时确认是否真正捕获到页面 JD；结果仍按页面 URL 缓存在 `chrome.storage.local`，切换页面后可恢复。
- 本轮验证：`npm run build:extension` 通过；岗位匹配/Manifest/Popup/Drawer 定向测试 10/10 通过；全扩展测试 48/48 通过；`npm run lint` 通过；本地服务接口冒烟确认“AI测试开发工程师”路由到测开简历且 JD 已进入 prompt。

## [2026-08-27] 岗位志愿排序与文本岗位限制

- 岗位新增可选的志愿排序字段，按从 1 开始的正整数记录，并在新增/编辑表单与岗位列表中显示“第 N 志愿”。
- 企业岗位限制改为字符串，支持保存并原样展示“限投 2 个岗位”“不限”等平台规则；空值显示“未知”。
- 旧 IndexedDB 岗位缺少志愿排序时自动补为空；旧 v1 备份缺少该字段仍可导入，旧的数字或空岗位限制会转换为字符串，IndexedDB 与备份版本均保持为 1。
- 移除已经不适用于文本规则的数值志愿上限计算，企业卡片直接展示岗位限制原文。
- `npm run lint`、81 项单元测试、83 项完整测试、插件构建及独立生产构建全部通过。


## [2026-08-20] 工作台岗位去重与插件投递状态

- 工作台移除筛选区域下方重复的岗位明细面板，保留统计、关键词、投递状态、截止时间和清空筛选；筛选结果直接作用于企业下方的正式岗位列表。
- 新增统一的十种投递状态契约和默认值，插件企业＋岗位模式可选择投递状态，默认“待投递”；仅企业模式不携带岗位状态。
- 插件页面缓存、失败草稿和重试均保留状态；AI 不会覆盖用户选择。管理页接收时执行状态白名单规范化，缺失或非法状态回退“待投递”。
- `npm run lint`、71 项单元测试、73 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] 仓库简历模板与自定义控件适配

- 新增仓库明文简历模板 `extension/resume-data/resume-profile.json`，预置 26 个常用 key，未获用户提供的敏感 value 保持为空；构建后随插件加载。
- 简历抽屉合并仓库模板、浏览器已保存资料和当前网页提取字段，并新增搜索、字段数量、来源和控件类型展示，适配大量字段维护。
- 扩展 Element、Ant、IVU 风格自定义下拉、日期、单选和复选控件识别；继续排除上传、验证码和提交控件。
- `npm run lint`、55 项单元测试、58 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] 简历资料库与网页一键填充

- 插件新增“维护简历信息”，以网页右侧抽屉维护 key-value 简历文档，支持提取当前页面字段、新增、修改、删除、本地保存和下次恢复。
- 支持从原生 input、select、textarea、radio、checkbox 提取稳定字段 key，并由用户点击“一键填充简历”后写入页面；明确排除文件上传、验证码、提交按钮、禁用及只读控件，不自动提交。
- 规则无法匹配时可由 DeepSeek 辅助映射网页字段 key 与简历 key；AI 请求不包含简历 value，映射结果经过字段白名单过滤。
- 新增版本化简历文档与平台 Schema 严格校验模块；同时修复仅企业模式错误禁用企业字段组的问题。
- `npm run lint`、50 项单元测试、53 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] 插件支持仅添加企业

- 插件新增“企业＋岗位 / 仅添加企业”提交模式；仅企业模式会禁用岗位区域，不再要求岗位名称。
- AI 未识别到岗位名称时自动切换为仅企业模式，识别到岗位时保持企业＋岗位模式，用户仍可手动切换。
- 管理页接收端支持只执行企业查重和写入，不创建空岗位；失败队列兼容显示仅企业草稿。
- `npm run lint`、35 项单元测试、37 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] 插件后台 AI 任务与提交反馈

- AI 识别从短生命周期弹窗迁移到扩展 Service Worker；关闭插件或切换页面后任务继续执行，回到同一招聘 URL 会恢复处理中、完成或失败状态。
- 按页面 URL 缓存 AI 结果及用户手动修改的表单内容，提交成功后清除对应缓存。
- “添加到投递台”成功后展示明确的“已提交”确认弹窗；自动打开管理页时不再抢占当前招聘页焦点。
- `npm run lint`、33 项单元测试、35 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] AI 招聘页识别与插件直传

- 插件新增 DeepSeek 设置和“AI 一键填写”，读取当前招聘页的裁剪 HTML，生成固定字段 JSON 并回填可编辑表单；AI 不执行最终提交。
- 用户点击“添加到投递台”后，插件打开或复用本地主应用页面，通过受确认的页面消息桥接写入 IndexedDB，并按域名、名称和岗位链接避免重复数据。
- API Key 仅存于 `chrome.storage.local`；同步失败的草稿保留在插件队列，可重新发送或删除。
- `npm run lint`、33 项单元测试、35 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] 浏览器插件空白弹窗修复

- 修复插件弹窗 HTML 乱码及标签损坏，恢复完整 UTF-8 中文表单、反馈区和待同步队列。
- 修复 Manifest 与插件交互提示中的乱码，并新增源文件、构建产物双重结构回归检查。
- 插件专项测试 10/10、仓库完整测试 30/30、代码检查、插件构建及独立生产构建全部通过；真实 Chrome 因当前自动化环境不可连接，需在扩展管理页重新加载后完成点击冒烟。

## [2026-08-13] 数据可靠性、投递仪表盘与浏览器采集插件

- 备份格式升级为严格 v1 契约校验，覆盖字段、类型、唯一 ID、岗位企业关联和版本检查；导入在同一 IndexedDB 事务中原子替换企业及岗位数据。
- 新增企业与岗位重复检测规则，支持规范化域名、URL、名称、简称及无 URL 岗位组合匹配。
- 主页面接入投递仪表盘，支持关键词、状态和截止窗口组合筛选，展示待投、进行中、Offer、已结束统计以及截止日期和志愿限制提醒。
- 新增 Chromium Manifest V3 采集插件，使用最小权限读取当前页面信息、录入企业与岗位草稿，并在 `chrome.storage.local` 中维护待同步队列。
- `npm run lint`、26 项单元测试、28 项完整测试、插件构建及独立生产构建全部通过。

## [2026-08-13] 并行开发基线加固

- 移除已废弃的 starter 预览组件，产品代码不再保留加载骨架残留。
- 将旧 starter 测试替换为求职投递台服务端渲染及核心父子条目、IndexedDB 流程冒烟测试。
- 修复企业卡片展开交互的键盘可访问性、外链安全属性及表单和存储边界类型。
- 统一以 npm 和 `package-lock.json` 作为依赖基线；`npm run lint`、`npm test` 和独立 `npm run build` 均通过。

## [2026-08-13] 企业分类两级导航

- 左侧导航调整为“企业性质 → 行业分类”两级结构。
- 支持一级展开、折叠和数量展示，并支持按一级或二级筛选。
- 生产构建验证通过。

## [2026-08-13] 求职投递台首版

- 建立企业父条目与岗位子条目的本地投递管理页面。
- 支持企业和岗位增删查改、搜索筛选、投递状态、志愿限制、截止日期、JD、简历版本及面试进度。
- 使用 IndexedDB 自动保存，预置 37 家重点国央企，支持 JSON 全量备份恢复。
- 生产构建验证通过。
## [2026-09-09] 岗位匹配改为本地 LLM 流式评测

- 岗位匹配取消 `codex run` 调用，改为本地 `job-fit-server` 直接调用浏览器插件设置中的 DeepSeek 兼容模型。
- 本地服务根据岗位 title 自动分类为“研发 / 产品 / 测试”，并写死读取对应简历路由：产品简历、Agent 开发简历、Agent 测开简历。
- 插件保存当前岗位描述，向本地服务提交 JD 与岗位表单信息，并在收到首个 LLM token 后立即渲染，持续把部分结果写入页面缓存。
- 本地服务只监听 `127.0.0.1:8765`，不保存 API Key；API Key 仅随本次本机请求从插件传给本地服务。

# 功能迭代文档

## [2026-09-09] 插件岗位匹配本地 Codex 服务

- 插件新增“岗位匹配”端到端 V1：读取当前招聘页 URL、标题、HTML 摘要及表单岗位信息，发送到本机 `http://127.0.0.1:8765/job-fit`。
- 新增 `npm run job-fit-server`，由本地 Node 服务执行 `codex run`，并在 Prompt 中明确复用 `C:\Users\37116\.codex\skills\JobFit-Interview-Master\SKILL.md`。
- 岗位匹配任务按页面 URL 缓存在 `chrome.storage.local`，关闭插件弹窗后回到同一 URL 可恢复处理中、完成或失败状态。
- 恢复插件弹窗、Manifest、README 的 UTF-8 中文文案，并补充源文件与 `extension/dist` 双重结构回归测试。
- 验证：`npm run build:extension`、`node --test extension/tests/*.test.mjs`、`npm run lint`、`npm run test:unit` 通过。
# [2026-09-09] 抽屉模式页面上下文桥接修复

- 修复右侧抽屉在得物等招聘站点中无法读取当前页面地址的问题；抽屉不再完全依赖 `chrome.tabs.query` 获取 URL。
- `extension/src/drawer.js` 新增页面上下文桥接：注入到招聘页后可直接读取当前页面 `location.href`、`document.title`、可见正文 `text` 与裁剪后的 `html`，并通过 `postMessage` 返回给抽屉 iframe。
- `extension/src/popup.js` 在抽屉模式下优先通过 `REQUEST_PAGE_CONTEXT/PAGE_CONTEXT` 获取页面上下文，失败时再回退到原有 tabs/content-script 方案。
- 页面正文采集会排除投递台抽屉自身节点，避免把插件表单文字误混入 JD。
- 本轮验证：`npm run build:extension` 通过；Drawer/AI/Capture/Popup 定向测试 9/9 通过；全扩展测试 49/49 通过；`npm run lint` 通过。

# [2026-09-09] AI 一键填写空回填修复

- 修复扩展 `ai.js`、`background.js`、`popup.js` 中的 UTF-8 乱码文案，避免错误提示、AI prompt 和用户反馈出现不可读内容。
- `AI 一键填写` 点击后会立即重新采集当前招聘页上下文，再启动后台任务，避免抽屉打开较早、页面后续加载 JD 后仍使用旧空缓存。
- DeepSeek 招聘页提取 prompt 新增“页面可见正文”输入，并保留 HTML 摘要兜底，提升对图中这类岗位详情页的公司、岗位、地点、批次和 JD 识别能力。
- AI 返回 JSON 若所有可编辑字段均为空，将被判定为识别失败并显示错误，不再出现“AI 已填写表单”但页面字段仍为空的假成功状态。
- 本轮验证：`npm run build:extension` 通过；AI/Popup/Drawer 定向测试 9/9 通过；全扩展测试 49/49 通过；`npm run lint` 通过。

# [2026-09-09] 岗位匹配 JD 捕获与浏览器流式输出修复

- 修复插件岗位匹配链路中的 UTF-8 乱码与损坏模板字符串，恢复 Manifest、抽屉 HTML、岗位状态、采集逻辑、弹窗提示与错误信息为可读中文。
- 页面采集脚本新增可见正文 `text` 输出，并继续保留裁剪后的 `html` 兜底；岗位匹配请求会把 title、URL、表单字段、正文文本和 HTML 摘要一起发送到本地 prompt 服务。
- 本地 `scripts/job-fit-server.mjs` 改为只负责读取写死简历路由、按岗位信息判断“研发 / 产品 / 测试”、构造岗位-简历匹配 prompt；LLM 请求由浏览器扩展按已配置 DeepSeek API Key 直接发起并读取 SSE token。
- 岗位匹配结果区新增 `JD 字符数`，便于冒烟时确认是否真正捕获到页面 JD；结果仍按页面 URL 缓存在 `chrome.storage.local`，切换页面后可恢复。
- 本轮验证：`npm run build:extension` 通过；岗位匹配/Manifest/Popup/Drawer 定向测试 10/10 通过；全扩展测试 48/48 通过；`npm run lint` 通过；本地服务接口冒烟确认“AI测试开发工程师”路由到测开简历且 JD 已进入 prompt。
