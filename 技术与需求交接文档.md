# 求职投递台：技术与需求交接文档

## 1. 项目目标

本地优先的求职投递管理工具。企业为父条目，岗位投递为子条目，统一管理企业资料、投递限制、岗位 JD、简历版本和面试进度。后续接入 Chromium 插件，从招聘页面录入数据。

## 2. 当前状态

已实现：37 家重点国央企初始数据；企业和岗位 CRUD；企业搜索；“企业性质 → 行业分类”两级筛选；投递状态、日期、JD、进度与简历版本；志愿数量；IndexedDB 持久化；严格 v1 JSON 导入导出与原子替换；企业及岗位重复检测；投递统计、组合筛选、截止提醒和志愿上限警告；筛选直接作用于正式企业/岗位父子列表且不重复展示岗位面板；响应式布局；Chromium Manifest V3 采集插件；插件岗位投递状态选择及白名单同步；DeepSeek 招聘页 HTML 识别与可编辑表单回填；Service Worker 后台 AI 任务；按招聘页 URL 恢复任务及表单；仅企业或企业＋岗位两种提交模式；用户确认后的插件到管理页消息桥接；仓库明文简历模板；支持搜索和来源展示的右侧简历资料抽屉；原生及部分 Element/Ant/IVU 自定义控件提取和用户触发填充；提交成功弹窗；发送失败草稿队列。

下一阶段：实际附件存储；面试进度时间线；通知级截止提醒；导入版本迁移；可选的服务端密钥代理和云端同步。

## 3. 技术架构

- React 19、TypeScript 客户端单页应用。
- vinext、Vite 构建，保持 Cloudflare 兼容输出。
- IndexedDB 数据库：`job-application-tracker`，版本 1。
- 当前无账号、后端 API、数据库服务或环境变量；D1、R2 均未启用。插件直接调用 DeepSeek 兼容接口，并通过扩展内容脚本与 `http://localhost:3000/` 管理页完成带回执的消息通信。

关键文件：`app/page.tsx`（UI、正式岗位筛选、插件消息接收与集成入口）、`app/domain/job-status.ts`（主应用投递状态契约）、`app/extension-bridge.ts`（插件草稿白名单校验和数据映射）、`app/components/jobs/ApplicationsDashboard.tsx`（统计与筛选控制器）、`app/storage.ts`（IndexedDB 与 CRUD）、`app/data/`（备份契约及重复检测）、`extension/src/job-status.js`（浏览器运行时状态适配器）、`extension/resume-data/resume-profile.json`（仓库明文简历模板）、`extension/src/resume-repository.js`（仓库模板加载校验）、`extension/resume-ui/resume-profile.js`（简历维护抽屉）、`extension/src/resume-fields.js`（网页字段提取、规则/AI 映射与填充）、`extension/src/resume-schema.js`（简历文档和平台 Schema 契约）、`extension/src/ai.js`（DeepSeek 调用与响应约束）、`extension/src/background.js`（后台 AI 任务）、`extension/src/storage.js`（设置、失败草稿与按 URL 页面缓存）、`extension/src/content.js`（页面采集、简历填充和消息回执）、`extension/`（插件源码、测试和构建）、`tests/`（产品及领域测试）。

## 4. 数据模型

`Company`：企业 ID、名称、简称、性质、行业、官网、招聘链接、描述、志愿限制、截止日期、备注和时间戳。

`Job`：岗位 ID、企业 ID、名称、批次、地点、类别、链接、JD、状态、投递及截止日期、进度、简历版本、备注和时间戳。

删除企业会级联删除岗位。除“待投递、放弃、拒绝”外的状态计入已投志愿。备份顶层格式固定为 `{ version: 1, exportedAt, companies, jobs }`；导入先完成严格校验，再使用一个覆盖 `companies` 和 `jobs` 的 `readwrite` 事务整体替换，保证同成同败。

## 5. 运行与测试


## 9. 2026-08-27 数据模型补充

- `Company.applicationLimit` 为字符串，保存招聘平台的岗位限制原文；空字符串表示未知。旧 IndexedDB 和旧 v1 备份中的 `number | null` 会分别规范化为数字字符串和空字符串。
- `Job.preferenceOrder` 为 `number | null`，表示从 1 开始的“第 N 志愿”；只接受正整数。旧 IndexedDB 或旧 v1 备份缺少该字段时规范化为 `null`。
- 企业卡片直接展示岗位限制原文；岗位表格与编辑表单展示和维护志愿排序。原数值型志愿上限计算已移除，描述型限制不做推测。
- 本次兼容变更不升级 IndexedDB `DB_VERSION`，备份格式仍为 v1；导入在原子替换前完成旧字段规范化和新字段校验。
- 当前完整测试为 83 项，单元测试为 81 项；`npm run lint`、`npm run build:extension`、`npm test` 和独立 `npm run build` 均已通过。
```powershell
npm install
npm run dev
```

默认访问 `http://localhost:3000/`。保持域名和端口一致，否则浏览器会使用不同 IndexedDB 数据空间。

项目统一使用 npm 与 `package-lock.json`，不得引入第二套锁文件。功能变更必须先设计正常、边界、错误和回归用例，再执行静态检查、相关测试与 `npm run build`；全部通过后才允许更新两份项目文档。现有命令为 `npm run lint`、`npm run test:unit`、`npm run build:extension`、`npm test`、`npm run build`。`npm test` 会先执行生产构建，再运行产品冒烟、数据契约、投递指标、统一状态契约、正式岗位筛选、插件状态缓存/重试、消息桥接、AI 响应约束、仓库简历模板、简历文档/平台 Schema、网页字段填充及弹窗标记完整性测试。当前完整测试共 73 项；插件弹窗检查同时覆盖源文件和 `extension/dist` 构建产物。

## 6. 限制与风险

- 清除网站数据会删除 IndexedDB 记录，应定期导出 JSON。
- JSON 导入为经过严格校验的全量原子覆盖；当前只支持 v1，后续格式变化需要显式版本迁移。
- 简历附件只记录文件名或版本，不保存文件内容。
- 初始企业描述为通用内容，招聘官网、志愿限制和截止日期需人工补齐。
- DeepSeek API Key 以明文保存在用户自己的 `chrome.storage.local`，不会进入仓库，但拥有本机浏览器配置访问权的主体仍可能读取；公共或不可信设备不应保存密钥。未来上线公共服务时应改用服务端代理。
- AI 读取的是移除脚本、样式、SVG 和 iframe 后且最多 120000 字符的页面 HTML；动态加载到 iframe、登录后接口或页面尾部之外的信息可能无法识别，用户必须在提交前核对表单。
- 页面表单和 AI 任务按去除 URL 片段标识后的完整地址缓存在 `chrome.storage.local`；成功提交会清除当前页面缓存。查询参数不同的链接视为不同页面，长期未提交的缓存当前不会自动过期。
- 主应用与插件分别使用 TypeScript 状态契约和浏览器 JavaScript 适配器；两者由自动化测试锁定为同一十值集合。未来新增状态时必须同时更新两端及契约测试，禁止单边扩展。
- 主应用必须运行于 `http://localhost:3000/` 或 `http://127.0.0.1:3000/`；插件只对 DeepSeek 和这两个本地地址声明主机权限。发送端只有收到管理页写入回执才删除失败队列记录。
- 简历资料目前以明文 key-value 保存在 `chrome.storage.local`，不应在公共或不可信设备使用；DeepSeek 辅助映射只发送网页字段元数据和简历 key，不发送 value。
- 用户已明确选择将简历模板明文保存在项目仓库；`extension/resume-data/resume-profile.json` 及后续 Git 历史可能包含敏感信息。抽屉保存只更新 `chrome.storage.local`，浏览器扩展不能直接回写仓库文件，仓库模板需手工编辑和提交。
- 简历自动填充仅支持当前页面的原生表单控件；文件上传、验证码、提交按钮、跨域 iframe、Shadow DOM、contenteditable 和高度定制的下拉/日期组件不在当前支持范围。填充后必须人工逐项审核并自行提交。
- 加载未打包插件后，源码或构建产物更新不会自动完成可靠的运行态刷新；需在 Chromium 扩展管理页重新加载 `extension/dist`。当前空白弹窗问题已通过自动化回归，但真实 Chrome 点击冒烟仍需在可连接的浏览器环境确认。

## 7. 开发约定

- 保持 ID 稳定；数据结构变化时升级 IndexedDB 版本并设计迁移。
- 删除和覆盖导入必须有明确确认。
- 开发使用 `skills/develop-and-verify`，测试通过后使用 `skills/record-project-docs`。

## 8. 项目通信与决策升级

项目采用会话 ID 进行跨开发任务通信：负责人 `019ff9aa-f240-77e1-a426-f001e5701f86`；开发人员 A `019ffa01-9bf6-77d2-8788-6dff6fc23157`；开发人员 B `019ffa02-2a57-73f2-9ea4-82f2e3c878fe`；开发人员 C `019ffa02-4eb9-7d23-8be9-bac3051e1384`。

遇到需求歧义、共享接口或依赖变更、数据迁移、跨分支冲突以及其他需要负责人决定的问题时，开发人员必须通过负责人的会话 ID 请求决策，并主动声明自己的身份和会话 ID。回答人必须通过请求人的会话 ID 回复，同时主动声明自己的身份和会话 ID。完整消息字段和模板以 `并行开发交接计划.md` 的“会话 ID 通信协议”为准。

开发完成后，开发人员必须通过负责人会话 ID 主动发送完成通知，附带分支、提交 SHA、测试证据、变更范围、风险和集成说明。开发人员自行声明完成不代表任务关闭；只有负责人通过该开发人员的会话 ID 返回验收通过结论后，任务才正式完成。详细模板以 `并行开发交接计划.md` 的“完成后的强制交付通知”为准。
# 2026-09-09 交接补充：插件岗位匹配本地服务

本次新增“岗位匹配”V1。插件弹窗点击“岗位匹配”后，后台 Service Worker 会将当前招聘页 URL、标题、裁剪 HTML 摘要和表单岗位信息提交到 `http://127.0.0.1:8765/job-fit`。本地 Node 服务通过 `codex run` 执行岗位适配分析，Prompt 明确要求复用 `C:\Users\37116\.codex\skills\JobFit-Interview-Master\SKILL.md`，并遵守只基于简历事实与 JD 证据评分的边界。

启动方式：在项目根目录运行 `npm run job-fit-server`，再重新构建并加载 `extension/dist/`。服务只监听 `127.0.0.1:8765`，请求体最大 256KB，单次 `codex run` 超时时间 6 分钟。插件 Manifest 新增本地桥接权限 `http://127.0.0.1:8765/*`。

关键文件：`scripts/job-fit-server.mjs` 提供本地 HTTP 桥；`extension/src/job-fit.js` 构建岗位匹配 Prompt 并调用本地服务；`extension/src/background.js` 管理后台任务；`extension/src/storage.js` 新增 `jobFitTask` 页面缓存；`extension/src/popup.js` 展示处理状态和结果；`extension/tests/job-fit.test.mjs`、`extension/tests/popup-markup.test.mjs`、`extension/tests/manifest.test.mjs` 覆盖接口、权限和弹窗结构。

验证结果：`npm run build:extension` 通过；`node --test extension/tests/*.test.mjs` 46/46 通过；`npm run lint` 通过；`npm run test:unit` 85/85 通过。真实端到端冒烟还需要用户本机确认 `codex` 命令可用，启动 `npm run job-fit-server` 后在 Chrome 重新加载插件并点击“岗位匹配”。
# 2026-09-09 交接补充：岗位匹配改为本地 LLM 流式评测

当前“岗位匹配”不再调用 `codex run`。插件弹窗点击“岗位匹配”后，直接用 `fetch` 调用 `http://127.0.0.1:8765/job-fit`，并读取响应 `ReadableStream`；本地服务每收到 DeepSeek SSE 增量内容就写回纯文本响应，前端收到首个 token 后立即渲染到结果框，并节流写入 `chrome.storage.local` 页面缓存。

岗位描述由插件在 `extension/src/job-fit.js` 中生成并随请求发送，包含公司、岗位 title、地点、批次、投递状态、URL、页面标题、裁剪 HTML 和备注。服务端 `scripts/job-fit-server.mjs` 按 title 规则判断岗位类型：包含“测试/测开/QA/质量/评测/test”走测试，包含“产品/PM/product”走产品，其余默认研发。写死简历路由为：产品 `resume_chen_ru.html`，研发 `agent开发简历.html`，测试 `agent测开简历.html`。

本地服务使用插件传入的 provider/model/API Key 调用 LLM；当前仅支持 DeepSeek 兼容接口。API Key 不写入文件，不进入岗位描述缓存，但会作为本机 HTTP 请求体发送到 `127.0.0.1:8765`。服务只监听本机回环地址，不主动暴露到局域网。

本轮关键文件：`scripts/job-fit-server.mjs`、`extension/src/job-fit.js`、`extension/src/popup.js`、`extension/src/background.js`、`extension/tests/job-fit.test.mjs`、`extension/tests/popup-markup.test.mjs`、`extension/README.md`。
# 2026-09-09 交接补充：抽屉模式页面上下文桥接

问题现象：在 `https://campus.dewu.com/578078/position/7680885671376718130/detail` 等招聘站点，右侧抽屉顶部提示“无法读取当前页面地址，请手动填写岗位链接”。根因是抽屉页面运行在 `chrome-extension://.../popup.html?mode=drawer` iframe 中，依赖 `chrome.tabs.query({ active: true, currentWindow: true })` 获取当前 tab URL 在部分页面/焦点状态下不稳定。

本轮修复：
- `extension/src/drawer.js` 作为注入到招聘页的脚本，直接读取页面自身的 `location.href`、`document.title`、可见正文和 HTML 摘要。
- 抽屉 iframe 通过 `window.parent.postMessage({ source: "goodjob-drawer", type: "REQUEST_PAGE_CONTEXT", requestId })` 请求页面上下文。
- `drawer.js` 返回 `{ source: "goodjob-drawer", type: "PAGE_CONTEXT", requestId, context }`，其中 `context` 包含 `url/title/text/html`。
- `extension/src/popup.js` 在 drawer mode 下优先使用该桥接上下文；若 1.2 秒内无响应，再回退到原有 `chrome.tabs.query + content.js` 方案。
- `drawer.js` 采集 HTML 时会排除 `#goodjob-application-drawer`，避免把插件抽屉自身内容混入招聘 JD。

影响范围：仅改变右侧抽屉采集当前页面上下文的优先路径；不改变 DeepSeek 设置、后台 AI 任务缓存、岗位匹配本地服务、投递台写入协议或 IndexedDB 数据模型。

验证结果：`npm run build:extension` 通过；`node --test extension/tests/drawer-interaction.test.mjs extension/tests/ai.test.mjs extension/tests/capture.test.mjs extension/tests/popup-markup.test.mjs` 9/9 通过；`node --test extension/tests/*.test.mjs` 49/49 通过；`npm run lint` 通过。

冒烟建议：重新构建并在 Chrome 扩展管理页重新加载 `extension/dist/` 后，打开得物岗位详情页，再点击扩展图标。顶部应显示类似“已读取 campus.dewu.com，正文 N 字”，不应再出现“无法读取当前页面地址”。随后再点击“AI 一键填写”或“岗位匹配”。

# 2026-09-09 交接补充：AI 一键填写空回填修复

问题现象：插件点击“AI 一键填写”后提示“AI 已填写表单”，但企业名称、企业简称、企业官网等字段仍为空。根因是 AI 提取链路允许全空 JSON 被当作成功结果处理；同时抽屉打开时缓存的页面上下文可能早于岗位详情正文加载完成，导致后台 AI 请求没有稳定携带可见 JD 文本。

本轮修复：
- `extension/src/ai.js` 恢复 UTF-8 中文 prompt 和错误信息，并将 `pageContext.text` 与 `pageContext.html` 同时传给 DeepSeek。
- `parseAiResponse` 只保留固定可编辑字段；当所有字段为空时抛出“AI 没有识别出可填写字段”，由后台写入 failed 状态，前端显示失败原因。
- `extension/src/popup.js` 在点击“AI 一键填写”和“岗位匹配”时都会先重新执行页面采集，刷新 `pageContext` 后再启动任务。
- `extension/src/background.js` 保留 action-driven drawer 注入架构，只修复后台 AI 失败提示和失败状态写入。

回归覆盖：
- `extension/tests/ai.test.mjs` 新增“全空 AI JSON 不得成功”和“DeepSeek 请求体必须包含页面可见正文与 HTML”的断言。
- 已验证 `npm run build:extension`、AI/Popup/Drawer 定向测试 9/9、全扩展测试 49/49、`npm run lint` 全部通过。

冒烟建议：重新构建并在 Chrome 扩展管理页重新加载 `extension/dist/` 后，在岗位详情页点击“AI 一键填写”。如果仍失败，优先查看顶部反馈：若提示“没有识别出可填写字段”，说明 DeepSeek 返回了空字段；若提示 API/权限错误，则检查扩展设置中的 API Key、当前页面权限和网络。

# 2026-09-09 交接补充：岗位匹配 JD 捕获与 LLM 流式架构

当前“岗位匹配”采用混合架构：浏览器扩展负责采集当前招聘页上下文与调用 DeepSeek，本地 Node 服务 `scripts/job-fit-server.mjs` 只负责构造 prompt 和选择本地简历路由，不再调用 `codex run`，也不再代持 API Key。

端到端流程：
1. 用户在招聘岗位页点击扩展图标，右侧抽屉通过 `src/drawer.js` 注入并加载 `popup.html?mode=drawer`。
2. `src/content.js` 响应 `CAPTURE_PAGE_CONTEXT`，返回 `title/url/html/text`；其中 `text` 是从 `main/[role=main]/article/.position-detail/.job-detail/.atsx-rich-text/body` 提取的可见正文，最大 60000 字符，`html` 是移除脚本、样式、iframe 后的兜底摘要。
3. 用户点击“岗位匹配”后，`src/job-fit.js` 将表单字段、页面正文与 HTML 摘要 POST 到 `http://127.0.0.1:8765/job-fit`。
4. 本地服务按岗位标题、页面标题和 JD 正文判断岗位类型：命中“测试/测开/QA/质量/评测/test”走测试简历，命中“产品/PM/product”走产品简历，其余默认研发简历；随后读取写死的本地 HTML 简历，拼接岗位 JD，返回 `{ kind, resumePath, jdChars, prompt }`。
5. 扩展在浏览器侧使用设置中的 DeepSeek API Key 发起 `chat/completions` 流式请求，按 SSE token 增量渲染到岗位匹配弹窗，并节流保存到页面缓存。

排障要点：
- 如果结果提示“未读取到 JD”或 `JD 字符数` 为 0，优先检查当前招聘页 JD 是否在 iframe、Shadow DOM 或需要滚动/登录后异步加载的区域；本轮已覆盖普通可见正文和 HTML 兜底。
- 如果仍出现 `[岗位匹配失败] fetch failed`，先确认 8765 端口是否跑的是新版服务；旧服务可能残留在后台。可关闭旧进程后重新运行 `npm run job-fit-server`。
- 修改扩展源文件后必须重新执行 `npm run build:extension`，并在 Chrome 扩展管理页重新加载 `extension/dist/`。

本轮验证：`npm run build:extension` 通过；`node --test extension/tests/*.test.mjs` 48/48 通过；`npm run lint` 通过；本地服务接口冒烟确认“AI测试开发工程师”返回 `kind=测试`、测开简历路由、`jdChars > 0` 且 prompt 包含 JD 关键词。
