# 求职投递台：技术与需求交接文档

## 1. 项目目标

本地优先的求职投递管理工具。企业为父条目，岗位投递为子条目，统一管理企业资料、投递限制、岗位 JD、简历版本和面试进度。后续接入 Chromium 插件，从招聘页面录入数据。

## 2. 当前状态

已实现：37 家重点国央企初始数据；企业和岗位 CRUD；企业搜索；“企业性质 → 行业分类”两级筛选；投递状态、日期、JD、进度与简历版本；志愿数量；IndexedDB 持久化；JSON 导入导出；响应式布局。

下一阶段：Manifest V3 插件；插件与管理页数据同步；实际附件存储；重复检测；进度时间线；截止提醒。

## 3. 技术架构

- React 19、TypeScript 客户端单页应用。
- vinext、Vite 构建，保持 Cloudflare 兼容输出。
- IndexedDB 数据库：`job-application-tracker`，版本 1。
- 当前无账号、后端 API、数据库服务或环境变量；D1、R2 均未启用。

关键文件：`app/page.tsx`（UI 与交互）、`app/storage.ts`（IndexedDB 与 CRUD）、`app/globals.css`（样式）、`app/layout.tsx`（根布局）、`tests/rendered-html.test.mjs`（产品冒烟测试）、`skills/develop-and-verify/SKILL.md`（开发测试流程）、`skills/record-project-docs/SKILL.md`（文档门禁）。已移除 `app/_sites-preview` starter 残留。

## 4. 数据模型

`Company`：企业 ID、名称、简称、性质、行业、官网、招聘链接、描述、志愿限制、截止日期、备注和时间戳。

`Job`：岗位 ID、企业 ID、名称、批次、地点、类别、链接、JD、状态、投递及截止日期、进度、简历版本、备注和时间戳。

删除企业会级联删除岗位。除“待投递、放弃、拒绝”外的状态计入已投志愿。

## 5. 运行与测试

```powershell
npm install
npm run dev
```

默认访问 `http://localhost:3000/`。保持域名和端口一致，否则浏览器会使用不同 IndexedDB 数据空间。

项目统一使用 npm 与 `package-lock.json`，不得引入第二套锁文件。功能变更必须先设计正常、边界、错误和回归用例，再执行静态检查、相关测试与 `npm run build`；全部通过后才允许更新两份项目文档。现有命令为 `npm run lint`、`npm test`、`npm run build`。当前 `npm test` 会先执行生产构建，再验证产品标题、核心 UI、父子条目入口和 IndexedDB 存储契约。

## 6. 限制与风险

- 清除网站数据会删除 IndexedDB 记录，应定期导出 JSON。
- JSON 导入目前全量覆盖，需增强校验和版本迁移。
- 简历附件只记录文件名或版本，不保存文件内容。
- 初始企业描述为通用内容，招聘官网、志愿限制和截止日期需人工补齐。
- 浏览器插件尚未实现；插件不能直接修改磁盘 HTML，需要共享存储或通信桥接。

## 7. 开发约定

- 保持 ID 稳定；数据结构变化时升级 IndexedDB 版本并设计迁移。
- 删除和覆盖导入必须有明确确认。
- 开发使用 `skills/develop-and-verify`，测试通过后使用 `skills/record-project-docs`。
